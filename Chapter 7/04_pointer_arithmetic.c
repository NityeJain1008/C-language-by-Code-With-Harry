#include <stdio.h>

int main(){
    
    // int a = 5 ; 
    // int * ptr = &a ; 

    // printf("the vale of a is : %d\n", *ptr) ;
    // printf("the address is %d\n", ptr) ;
    // ptr++ ;

    // printf("The value of ptr is %d\n", ptr) ;

    char a = 'as' ; 
    char * ptr = &a ; 

    printf("the vale of a is : %u\n", a) ;
    printf("the address is %u\n", ptr) ;
    ptr++ ;

    printf("The value of ptr is %u\n", ptr) ;
    return 0;
}